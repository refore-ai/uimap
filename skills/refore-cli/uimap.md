# UIMap MCP Server

Start the UIMap MCP server to allow AI agents to interact with UIMap services over the MCP protocol.

## Prerequisites

Refore credentials must be configured. See [credential.md](./credential.md).
